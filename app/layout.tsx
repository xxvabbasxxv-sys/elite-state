import './styles.css'
export const metadata = { title: 'QBCore Control', description: 'Private FiveM administration' }
export default function Layout({ children }: { children: React.ReactNode }) { return <html lang="ar" dir="rtl"><body>{children}</body></html> }
